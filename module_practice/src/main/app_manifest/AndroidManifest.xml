<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.module_practice">

    <application
        android:name="example.common_base.app.MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.PracticeRoomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.LocalAccompanimentActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.SingleSingActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.SingModeActivity"
            android:launchMode="singleTop" />

    </application>

</manifest>